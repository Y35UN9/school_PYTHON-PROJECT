class Score():
    def __init__(self, scoreboard_img,now_score = 0,best_score = 0):
        self.now_score = now_score
        self.best_score = best_score
        self.scoreboard_img = scoreboard_img

        def nowScan(self):
            pass #checkline 만들고

        def bestScore(self):
            if now_score > best_score:
                best_score == now_score